[package]
name = "update"
version = "0.1.0"
authors = ["Jake Shadle <jake.shadle@embark-studios.com>"]
edition = "2018"
publish = false

[dependencies]
failure = "0.1.6"
reqwest = { version = "0.10.3", default-features = false, features = ["rustls-tls", "blocking", "json"] }
serde_json = "1.0.48"
